import { InlineSvgDirective } from './inline-svg';

describe('InlineSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineSvgDirective();
    expect(directive).toBeTruthy();
  });
});
